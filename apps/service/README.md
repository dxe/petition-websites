# Email petition service

For helptheducks.com, helpthechickens.com, freezoe.org, and righttorescue.com.

Run the dev server:
`go run .`

## Configuration

The service uses environment variables for configuration. Environment variables are loaded from both files when using VS Code debugger